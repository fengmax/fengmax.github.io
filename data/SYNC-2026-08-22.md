# SYNC 2026-08-22

- commit `9c93def`（代码） → `36b26f4`（merge，远端）
- 改动清单 v1：
  提速：贴图转 WebP + 预加载 740K 引擎并行下载
  - 9 张 -hi + 9 张 -normal + earth-specular + skybox 全部转 WebP（q82）
  - moon-hi 降采样 2048→1024（小球屏占比小，零感知损失）
  - 贴图总字节 2352K → 1522K（-35%）
  - index.html 加 7 张最重贴图 `preload`，`crossorigin="anonymous"` 匹配 THREE
  - scene-init.js 5 处引用 .jpg → .webp
  验证：headless 无 404 / 无纹理加载错误；preload credential 警告已归零；场景渲染正常

- commit `c2fd656`（回退 preload，远端）
- 回退原因：preload 反而更慢（用户反馈 + CDP 限速实测证实）
  - 根因：7 张 preload 占满同源 6 个连接槽并瓜分带宽，饿到关键 `spacekit.js`（740K）下载被拖慢
  - 100KB/s 实测：spacekit.js 结束 18684ms(有preload) vs 9037ms(无preload) → 场景出现慢一倍
  - 修复：删除 7 张 preload，保留 WebP 体积收益；场景出现比原始 JPG 版更快且无争抢
  - 教训：关键 JS 之前不要 preload 非关键大图；本地无限带宽测不出连接争抢，须 CDP 限速(非 localhost)测关键路径

- commit `0b88835` → `34e58ac`（merge，远端）
- 改动清单 v2（提速第二轮，用户反馈仍慢）：
  - 修加载层：去掉 600ms/2000ms 固定计时器，改 viz 创建+渲染首帧后(requestAnimationFrame)隐藏；慢网下不再出现"加载层消失后长时间空白黑页"（这是之前最像"卡死/没优化好"的观感元凶）
  - 法线贴图延迟 1s 加载(setTimeout)：给 -hi 主贴图留带宽先行窗口；requestIdleCallback 在下载期主线程 idle 会立即触发不顶用，故用固定 setTimeout
  - 修正：原想跳过月球法线(NSTR.moon=0)，但 `NSTR[key]||0.85` 回退使月球法线实际 0.85 有用，故不跳过、移除恒真守卫
  - 验证：headless 无报错、loading 正常隐藏(display:none)、body.loaded、canvas+贴图正常渲染

- commit `550561d`（LOD，远端）
- 改动清单 v3（默认低清，按需升级高清）：
  - `scene-init` 默认加载 `{key}.webp` 低清（moon + 动态两处）；9 张低清 jpg 同步转 webp(q78) 并删原 jpg
  - 修 `app.js` 上轮转 webp 漏改的 4 处引用：forceHiRes/checkTextures 的 `-hi.jpg`→`-hi.webp`，preloadTextures/checkTextures 的 `.jpg`→`.webp`
  - 移除冗余 `preloadTextures()`（避免低清双倍下载）；初始化 `texLoaded` 标记启动已加载低清，防止首帧 checkTextures 重复拉取
  - 行为：全景默认低清（~104KB 而非 ~1.5MB），放大到行星附近时 `checkTextures` 按距离自动切 `-hi.webp` 高清（按需下载）
  - CDP 抓包验证：启动资源清单中无 `-hi.webp`，仅 9 张 `{key}.webp` + 法线（延迟 1s）+ 天空盒 + specular
  - headless 渲染：全景下行星为小点（低清够用）、太阳/银河/轨道环/新闻面板正常、无 404/无报错

- commit `75dd5bf`（新闻改方案C：单条快照卡 + 进度环，远端）
- 改动：
  - 删旧滚动列表（news-track / newsScroll / 双份复制无缝循环）改为单条快照卡：来源色点+名 / 相对时间 / ✕ / 标题(2行) / n·总条数 / 5秒倒数的 SVG 进度环
  - 进度环用 Web Animations API 驱动 `strokeDashoffset 0→C(94.25)` over 5s，onfinish 自动换下一条；hover 暂停/离开恢复；退化用 setTimeout
  - 来源配色：NASA 蓝 / Space.com 琥珀 / The Verge 粉 / IT之家 绿 / cnBeta 紫——点+进度环统一色
  - 标题切加 ncFade 淡入(0.42s) + min-height 防卡片跳动
  - 顺手修旧版遗留 bug：init 没设胶囊初始 .hidden，导致面板开着时胶囊也露着

