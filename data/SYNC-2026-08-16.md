# Sync marker — 2026-08-16 11:55 (Asia/Shanghai)

记录「本地桌面版 ↔ GitHub Pages 已对齐」的同步事件,日后扫一眼本目录就知道何时对齐的。

## 状态

- **本地源:**  `C:\Users\fengl\Desktop\太阳系沙盒.html`  (59,236 字节, CRLF 行尾)
- **远程目标:**  `fengmax/fengmax.github.io` main 分支 `index.html`  (57,704 字节, LF 行尾)
- **行尾差异:**  1,537 行 × 1 字节 CR = 1,537 字节差额
- **代码内容差异:**  **0** — 去除行尾符后两份文件字节级别一致

## 本次同步 commit

- **commit SHA:**  `7739fdfd35a0d7009d2b438e5e89c60267382b71`
- **message:**  `月球 label 改为 HTML overlay + 缩小到 R=0.10 + 修首帧 set(0,0,0) + 轨道环颜色亮度`
- **index.html SHA:**  `587e15642dc3a71ed8341585c814e066a027c3a9`
- **size:**  57,704 bytes
- **url:**  https://github.com/fengmax/fengmax.github.io/commit/7739fdfd35a0d7009d2b438e5e89c60267382b71

## 本次月球改动一览

1. **月球 label 改为 HTML overlay**(不再用 SpaceKit 默认 SVG label)
   - 新增 `<div class="moon-overlay-label hidden" id="moon-overlay-label">🌙 月球</div>`
   - 每帧在 `updateMoonOrbit()` 末尾把月球 mesh 世界坐标 → 屏幕坐标 → 投影
   - `z > 1 / z < -1` 时(相机背后)自动隐藏
   - `transform: translate(-50%, -100%)` 锚定在月球上方
2. **月球大小再微调**  `Math.max(moonRadiusScaled * 1.0, 0.12)` → `Math.max(moonRadiusScaled * 1.0, 0.10)`
3. **修首帧闪烁**  `meshes[0].position.set(12, 0, 0)` → `meshes[0].position.set(0, 0, 0)`
4. **轨道环颜色亮度**  `0x9fc8ff` opacity `0.55` → `0xbfe2ff` opacity `0.78`
5. **去掉 moon createSphere 的 labelText**  让 SpaceKit 不再自动生成 SVG label,与自定义 HTML overlay 互不干扰

## 同步策略(以后沿用)

1. 本地用 WorkBuddy AI 编辑 `太阳系沙盒.html`
2. 用 Python 字节级归一化:C:\Users\fengl\AppData\Local\Temp\solar_index_lf.html(去 BOM + CRLF→LF)
3. 通过 `mcp__github__create_or_update_file` 增量提交(必须带 `branch` / `path` / `sha`)
4. 顺手创建/更新这个 marker 文件,确保 commit history 不留空记录
5. GitHub Pages 自动构建,几个 commit 之内 `https://fengmax.github.io/` 上看得到效果

## 上次对齐时间

- 2026-08-16 11:45 — 同步 round 1(轨道环 + Kepler 模型)
- 2026-08-16 11:55 — 同步 round 2(HTML overlay + 大小微调 + set(0,0,0) + 环色)