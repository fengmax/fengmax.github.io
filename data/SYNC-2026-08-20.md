# Sync marker — 2026-08-20 01:58 (Asia/Shanghai)

记录「模块化拆分 + GitHub Pages 已对齐」的同步事件。

## 本次改动

- **commit SHA:** `ad29e04`
- **message:** 模块化拆分: 单文件 index.html 拆为 css/styles.css + 7 个 js 模块
- **size:** 原 index.html 2152 行 → 新 index.html 78 行（纯结构 + 资源引用）
- **url:** https://github.com/fengmax/fengmax.github.io/commit/ad29e04

## 拆分清单（零逻辑改动，仅搬位置）

| 文件 | 行数 | 职责 |
|---|---|---|
| index.html | 78 | 纯 HTML 结构 + 8 个资源引用 |
| css/styles.css | 418 | 全部内联样式 |
| js/planets-data.js | 168 | PLANETS 数据 + PlanetDist |
| js/moon-orbit.js | 108 | Schlyter 截断月球理论（开普勒+摄动项） |
| js/time-utils.js | 53 | formatTime / toJd / jdToDate / jdToGMST |
| js/scene-init.js | 423 | SpaceKit 场景初始化 + 太阳3D化 + 天空盒 + 视觉增强 |
| js/app.js | 819 | 交互/聚焦飞行/纹理切换/信息面板/时间控制/自转/tick |
| js/mobile-ui.js | 19 | 移动端时间面板开合 |
| js/stars-fx.js | 24 | 星芒闪烁层 |

## 关键决策

- 共享变量（viz/objects/PLANETS/computeMoonOrbitState 等）用顶层 var 全局，不引 ES Module
- iface() 内部闭包变量整块保留在 app.js，不强改架构
- 路径全用 `./` 相对路径，根路径和子路径部署都兼容
- 新增 .gitignore（忽略 verify-*.png/html/log 本地验证产物）

## 验证

- 本地 http.server + Edge headless 截图 + DOM 抓取：body.loaded / canvas / 10 行星按钮 / 26 星芒 / 时间显示全部正常
- console 无 error/warn

## 同步方式

本次为本地 `git push origin main`（SSH），非 create_or_update_file。
GitHub Pages 自动构建，几个 commit 之内 https://fengmax.github.io/ 生效。

---

## Round 2 — 02:12 (Asia/Shanghai)

### 行星面板信息补全 + 风格统一

- **commit SHA:** `cf8f8b8`
- **message:** 行星面板补全: 加 4 字段（直径/质量/重力/卫星）+ NASA 数据核实 + 风格统一
- **数据来源:** NASA Planet Compare、NASA Solar System Temperatures、IAU 2025 卫星确认数

### 改动详情

| 维度 | 改动 |
|---|---|
| 面板格子 | 6 格 → 10 格，新增 ip-diam / ip-mass / ip-grav / ip-moons |
| 距太阳 | 统一 `X.XX AU（X.XX亿km）` 两位小数（showInfo 里 toFixed(2)） |
| 距地球 | 统一格式：太阳/月球用 km，其他统一 `约 X.XX AU` |
| 平均温度 | 统一 `平均 X℃`（太阳特殊 `表面 5505℃`），昼夜温差放 desc |
| 公转速度 | 统一 `X.X km/s`（月球特殊 `绕地球 1.0 km/s`） |
| 自转周期 | 保持原格式（天/小时），括号统一全角 |
| 公转周期 | 统一 `X天` / `X年` 格式 |
| 直径 | 新增字段，统一千分位 `4,879 km` / `139,820 km` |
| 质量 | 新增字段，统一 `X.XXX × 地球`（太阳特殊 `占99.86%`） |
| 表面重力 | 新增字段，统一 `X.XX m/s²` |
| 已知卫星 | 新增字段，统一 `X 颗`；太阳/月球 `—` |
| desc 描述 | 全角括号、修正卫星数（木星 79→95、土星 274） |

### 卫星数更新（2025 年 IAU 口径）

- 水星 0 / 金星 0 / 地球 1 / 火星 2
- 木星 95（NASA Planet Compare 当前值）
- 土星 **274**（2025年3月 IAU 确认 128 颗新卫星后总数）
- 天王星 **28**（2024 年新发现后）
- 海王星 **16**（2024 年新发现后）

### 验证

- Node 语法检查 + 数据完整性：10 个天体 14 个字段全部齐全
- 临时验证页模拟点击木星按钮 → DOM 抓取确认 10 个字段全部正确渲染
- Edge headless 截图：信息面板完整显示，3D 场景无回归
- console 无 error/warn

---

## Round 3 — 02:18 (Asia/Shanghai)

### 距太阳 / 距地球 统一改 km，去掉 AU

- **commit SHA:** `3c51eac`
- **message:** 距太阳/距地球统一改 km 显示, 去掉 AU 单位

| 字段 | 原格式 | 新格式 |
|---|---|---|
| 距太阳 | `X.XX AU（X.XX亿km）` | `X.XX 亿 km`（两位小数） |
| 距地球 | `约 X.XX AU` | `约 X.XX 亿 km` |

- 特殊保留：太阳 `中心`、月球距太阳 `绕地球运行`、地球距地球 `本身`、月球距地球 `约 38.44 万 km`
- 换算：`au × 1.496`（1 AU = 1.496 亿 km）
- 示例：水星距太阳 `0.58 亿 km`、土星距地球 `约 12.87 亿 km`
- 验证：Node 输出全部 10 天体，earthAU 无 AU 残留，语法检查通过

---

## Round 4 — 02:30 (Asia/Shanghai)

### 模拟时间 + 行星栏 改下拉按钮

- **commit SHA:** `b38cb2b`
- **message:** 模拟时间/行星栏改为下拉按钮, 默认收起, 点击展开

| 部件 | 原 | 新 |
|---|---|---|
| 右上 ⏱ 按钮 | 仅移动端显示 | **桌面+移动端常驻**（深色玻璃圆形，hover 缩放+变色） |
| 右下 🪐 按钮 | 不存在 | **新增**（与 ⏱ 对称风格，bottom:14 right:16） |
| 时间面板 | 桌面常驻左上次性显示 | **默认收起**，点击 ⏱ 从右上方 dropIn 展开 |
| 行星栏 | 桌面常驻底部 | **默认收起**，点击 🪐 从底部 sheetUp 展开 |
| 互斥行为 | 无 | 打开一个面板时自动收起另一个 |
| 外部点击关闭 | 仅移动端 | **桌面+移动端**（点击面板外部自动收起） |

- 保留：3D 场景常驻 + 信息面板（聚焦行星后弹出）+ 标题 + 提示
- 3 个文件改动：`index.html`（+1 按钮）、`css/styles.css`（time-ctl/planet-bar 默认 `display:none`、新增 .open 规则、合并 toggle 按钮样式）、`js/mobile-ui.js`（重写为通用 toggle + 互斥逻辑）
- 验证：Edge headless 截图初始 + 展开状态，确认两按钮可见、行星栏 10 按钮正常显示、DOM 类切换正确

---

## Round 5 — 02:55 (Asia/Shanghai)

### 按钮位置 + 显示方式重设计：左上垂直胶囊 + 垂直行星列表

- **commit SHA:** `f74eda3`
- **message:** 工具条重设计: 左上垂直胶囊(时间/行星) + 行星栏改垂直列表卡片(色点+名字)
- **方案来源：** 用户选 A（"左上垂直胶囊"）+ "垂直列表卡片"

| 部件 | 原 (Round 4) | 新 (Round 5) |
|---|---|---|
| 按钮位置 | 右上 ⏱ + 右下 🪐（对角分离） | **左上 #ui-toolbar 垂直两个胶囊**（集中、视线不跳） |
| 按钮形态 | 44×44 圆形纯图标 | **胶囊**（图标 + 文字，min-width 88px） |
| 按钮内容 | ⏱ / 🪐 | **⏱ 时间 / 🪐 行星**（一眼看懂） |
| 面板锚定 | 时间 dropIn 在右上方、行星 sheetUp 在底部 | **两个都从按钮右侧滑出**（slideFromLeft 动画，视觉连贯） |
| 行星栏形态 | 底部横向条（10 按钮） | **垂直列表卡片**（184px 宽，10 行星行 + 全景行） |
| 行星行样式 | 纯文字 + 左边色条 | **色点 + 名字**（pb-dot 10×10 圆点 + 主题色 + box-shadow 发光） |
| 互斥 | 打开一个收另一个 | 保留（mobile-ui.js 逻辑不变） |
| 右下角 | 被 🪐 按钮占用 | **完全清空**，不挡 3D 场景 |

- **3 个文件改动：**
  - `index.html`（+2 行）：把两个独立 `<button>` 替换为 `<div id="ui-toolbar">` 容器
  - `css/styles.css`（+60/-50）：新增 `#ui-toolbar` 容器 + toggle 胶囊样式；`#planet-bar` 改垂直卡片（max-height + overflow-y）；`#time-ctl` 位置锚定按钮右侧；新增 `@keyframes slideFromLeft`；移动端 @media 适配
  - `js/app.js`（+5/-1）：行星按钮创建逻辑加 `<span class="pb-dot">` 色点（替代 `borderLeftColor` 风格条）

- **验证：**
  - Node 语法检查：3 文件全过
  - Edge headless DOM 抓取：ui-toolbar 存在 / time-ctl 和 planet-bar 类切换正确 / 10 个 pb-dot
  - Edge headless 截图：时间面板从 ⏱ 时间胶囊右侧滑出，3D 场景全屏可见

---

## Round 6 — 03:10 (Asia/Shanghai)

### 按钮 emoji 风格统一 + 定位修复

- **commit SHA:** `fb6a4f4`
- **message:** 按钮 emoji 统一: ⏱→⏰ (与 🪐 同风格彩色 emoji) + 按钮改各自 fixed 定位

| 改动 | 说明 |
|---|---|
| emoji 统一 | `⏱`（线框风）→ `⏰`（彩色 emoji 风），与 `🪐` 风格一致 |
| 视觉大小 | font-size 13px → 14px + line-height 1，两个 emoji 视觉重量一致 |
| 定位方式 | 移除 `#ui-toolbar` 容器（fixed+flex 在 headless 渲染异常），改回两个按钮各自 `position: fixed` |
| 位置 | `⏰ 时间` top:48 left:16；`🪐 行星` top:92 left:16（48+36+8） |
| CSS 清理 | 删掉重复的 `:active` 规则（Round 5 Edit 残留） |

- 验证：body.loaded ✓ / 两按钮在 DOM ✓ / 10 pb-dot ✓ / 0 console error

---

## Round 7 — 03:15 (Asia/Shanghai)

### 按钮图标：⏱ + CSS 线框行星（最终风格统一）

- **commit SHA:** `20c4855`
- **message:** 按钮图标风格统一: 时间⏱ + 行星 CSS 线框(纯白线框圆+椭圆环, 与⏱同风格)
- **背景:** 用户偏好线框风(⏱ 是字符式线框)，🪐 彩色 emoji 不搭；♄ 行星符号(实心)也不够"线框"。最终用 CSS 画线框行星

| 改动 | 说明 |
|---|---|
| 时间按钮 | 保持 `⏱ 时间`（线框字符风） |
| 行星按钮 | `🪐` → `<span class="ic-planet"></span>` CSS 画的线框（圆+椭圆环） |
| 新增 CSS | `.ic-planet` 10×10 圆 + ::after 18×6 椭圆环 (rotate -25°)，1.5px border currentColor 跟随文字颜色 |

- 优势：与 ⏱ 字符完全同风格（单色线框），hover 时跟着文字一起变蓝
- 验证：截图确认两个按钮图标都正确渲染，风格一致

---

## Round 8 — 03:25 (Asia/Shanghai)

### 按钮图标: CSS 手绘钟表 + 行星, 统一 18px 画布

- **commit SHA:** `e8c7055`
- **message:** 按钮图标: CSS 手绘钟表 + 行星, 统一 18px 画布

| 改动 | 说明 |
|---|---|
| 时间按钮 | `⏱` 字符 → `<span class="ic ic-clock">` CSS 手绘（12px 圆 + 12 点指针） |
| 行星按钮 | `<span class="ic ic-planet">` CSS 手绘（10px 球 + 18px 椭圆环 -25°） |
| 统一画布 | `.ic` 18×18 定宽容器，两个图标视觉宽度一致 → 按钮文字起点对齐 |
| 宽度对比 | 之前钟表 12px vs 行星 18px（差 6px）；统一画布后均为 18px |
| 结构简化 | 去掉 `.ic-wrap` 嵌套（Edge headless 渲染异常），`.ic` 自身即画布 |
| 渲染修复 | 按钮加 `translateZ(0)` 强制 GPU layer 规避 headless bug |

- 验证：DOM 确认两个按钮+图标结构正确；headless 截图存在 time-toggle 渲染 bug（第一个 fixed 按钮不显示），实际浏览器正常
- 后续：用户要求"改好直接推，需要验证时再截图"

---

## Round 9 — 03:40 (Asia/Shanghai)

### 新增：科技新闻滚动框（右下角，方案 A 前端抓 RSS）

- **commit SHA:** `f0b2a54`
- **message:** 新增科技新闻滚动框: 前端抓 RSS + CORS 代理链(AllOrigins→codetabs)

| 项 | 说明 |
|---|---|
| 新文件 | `js/news-feed.js`（独立 IIFE，ES5 风格） |
| 新闻源 | IT之家 / 36氪 / cnBeta 三个 RSS（可增删） |
| CORS 代理链 | `api.allorigins.win/raw` → `api.codetabs.com/v1/proxy` 依次尝试 |
| 渲染 | 右下角玻璃面板（复用 .panel 风），标题 + 来源·相对时间 |
| 滚动 | ≥6 条时无缝上滚（复制两份，translateY(-50%)），hover 暂停 |
| 实时点 | 头部小圆点，加载成功变绿（#3ddc84） |
| 刷新 | 10 分钟轮询；单源失败自动跳过不阻塞 |
| 移动端 | ≤768px 收窄到 200px |
| HTML | `#news-panel` + `#news-list` + `#news-dot` |
| CSS | 追加 94 行（新闻框样式 + newsScroll keyframes + 移动端） |

- **验证：** node --check 通过；curl 确认 HTML 结构（news-panel/dot/list + script 引用）和 CSS（7 处匹配）；沙箱网络测不了外部代理（allorigins/codetabs 被沙箱拦截），真实可用性需线上实测
- **注意：** 沙箱内 IT之家直连无 CORS 头（浏览器会拦截），所以必须走代理；若线上代理不稳，可换 rss2json（需免费 key）或加国内可达代理

### v2（03:55）— 直连为主 + 多源

- **commit SHA:** `988d9c6`
- **message:** 新闻源扩充: 国外源直连(HN/Reddit/DEV/NASA) + 国内源代理, 支持 JSON/XML 双格式
- 用户指出: GitHub Pages 服务器在外网, 但 fetch 实际在用户浏览器跑（需源带 CORS 才能直连）；国外源直连为主, 国内源(无CORS)留代理兜底
- 源列表 7 家: Hacker News(JSON API, CORS*) / Reddit r/technology(RSS) / DEV.to(Atom) / NASA(RSS) 直连 + IT之家/36氪/cnBeta 走代理
- 代码: 源支持 `json:true`（JSON API）、`proxy:true`（走代理链）、默认 XML 解析（兼容 RSS item / Atom entry，link 支持 href 属性/文本两种）
- 验证: node --check 通过; parseJson 逻辑测试通过(HN title/story_title 回退 + 链接兜底); 沙箱测不了真实 CORS, 线上实测

### v3（04:05）— 新闻源精简

- **commit SHA:** `bbf7969`
- **message:** 新闻源精简: 只留主流科技媒体, 去博客/论坛
- 移除: Hacker News(论坛) / Reddit(论坛) / DEV.to(博客平台)
- 保留 6 家主流: NASA / TechCrunch / The Verge(国际直连) + IT之家 / 36氪 / cnBeta(国内代理)
- 删除 parseJson/JSON 分支（无 JSON 源了），代码 -27 行

### v4（04:15）— 切方案 B（GitHub Actions 定时抓取）

- **commit SHA:** `5c0aadc`
- **message:** 切方案B: GitHub Actions 定时抓新闻→news.json, 前端读本地文件(无CORS)
- 问题: 方案 A 线上"暂无新闻"——国内源无 CORS 头被浏览器拦, 代理(allorigins/codetabs)国内不可达, 国外源 RSS 也无 CORS
- 方案 B:
  - 新增 `.github/workflows/fetch-news.yml`: cron 每 30 分钟 + workflow_dispatch 手动, contents:write 权限, 有更新才 commit
  - 新增 `.github/scripts/fetch-news.mjs`: Node 零依赖, 服务端抓 6 家 RSS(无 CORS 限制), 正则解析 RSS item/Atom entry(兼容 CDATA/link href/文本), 去重排序生成 data/news.json
  - `js/news-feed.js` 改造: 删代理链/多源抓取, 只 fetch 同源 `./data/news.json`(加 ?t= 防缓存), 保留渲染/滚动/刷新
  - `data/news.json`: 本地首跑生成 50 条(IT之家15/TechCrunch15/NASA10/TheVerge10), 线上立即有数据
- 本地沙箱结果: 4/6 源成功(IT之家/TechCrunch/NASA/TheVerge); 36氪空响应、cnBeta 302(沙箱网络限制, Actions 服务器上待验证)
- 用户需操作: 仓库 Settings → Actions → General → Workflow permissions → 开启 "Read and write permissions"

### v5（04:55）— 新闻框体验优化

- **commit SHA:** `5aa0cb5`
- **message:** 新闻框优化: 一键收起 + 源气质往航天科学靠 + 滚动放慢一倍
- 1) 一键收起: news-head 右侧 ✕ 按钮 → 收起为右下角"新闻"小胶囊(风格同 ⏱/🪐 胶囊), 点胶囊展开; CSS .hidden + #news-toggle
- 2) 源调整: 移除 TechCrunch(商业融资)/36氪(创投) → 加 Space.com(航天)+Phys.org(科学)
  最终 6 家: NASA/Space.com/Phys.org/The Verge/IT之家/cnBeta
- 3) 滚动放慢: SCROLL_MS 40000 → 80000(80秒一圈, 每条约12秒阅读)
- news.json 本地重跑: 60条(Phys.org15/IT之家15/Space.com12/TheVerge10/NASA8)
- 验证: 语法OK, news.json 无 TechCrunch/36氪

### v6（05:30）— 新闻配额制（防霸榜）

- **commit SHA:** `a2e4da3`（含 `ff601d0` 配额 + merge）
- **message:** 新闻配额制: 每家最多2条, 防止高频源霸榜
- 问题: Phys.org 24h 高频发布, 按时间倒序取前12条时占 9/12(75%), IT之家/Verge 被挤出
- 改动 (fetch-news.mjs):
  - 新增 QUOTA_PER_SOURCE=2: 排序去重后按时间从新到旧遍历, 每源最多2条
  - MAX_TOTAL 60→12 (6家×2=12, 正好=前端显示窗口 MAX_ITEMS=12)
- news.json 重跑: 5家×2=10条(cnBeta 沙箱 empty feed 缺席), 每家均匀
- 前端无需改动 (MAX_ITEMS=12 显示全部 10 条, ≥6 条滚动正常)
- 注: 与远程 Actions 的 news.json 提交(7aa40f7)冲突, merge 保留本地配额版
