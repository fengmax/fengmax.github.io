# SYNC 2026-08-27

- commit `fbd5f29`（远端）
- 改动：太阳模型改回官方默认 lensflare 精灵
  - `js/scene-init.js` 删除 90 行自定义"太阳 3D 化"代码块（自绘白核球 + 柔光 sprite + 接管 _object3js 的逻辑）
  - 恢复为官方默认 `viz.createObject('sun', Spacekit.SpaceObjectPresets.SUN)` → SpaceKit 静态精灵（`assets/sprites/lensflare0.png`，AdditiveBlending）
  - 官方预设 `SUN: { textureUrl: "{{assets}}/sprites/lensflare0.png", position:[0,0,0] }`，无 ephem → `createSprite()`，scale [1,1,1]
  - 验证：headless 无报错、body.loaded、canvas 正常渲染

- commit `76569f7`（远端）
- 改动：去掉太阳官方 lensflare 的黄色光晕 + 十字光芒
  - 用户反馈"太阳外面那圈黄的是什么，能去掉么"
  - 查 SpaceKit 源码：官方默认 `lensflare0.png` 是相机镜头光晕（白核 + 黄色光晕 + 十字光芒），是贴图内容
  - 解法：保留官方 sprite（scale/blending/position 都对），只替换 `material.map` 为程序生成的干净白核贴图（径向渐变 纯白→透明，无黄、无十字）
  - 同时把 sprite `scale` 从官方默认 `[1,1,1]` 放到 `[3,3,1]`（换贴图后太小看不见）
  - `js/scene-init.js` 在 `objects.sun = createObject(...)` 后加 setTimeout 替换贴图（约 28 行新增）
  - 验证：headless 无报错、console 干净；贴图颜色全部 `rgba(255,255,255,*)`，逻辑上不可能出现黄色

- commit `9f659f4`（远端，Revert `76569f7`）
- 改动：用户要求回退到上一个版本 → 撤销"去太阳黄圈"
  - `git revert 76569f7`（不用 reset --hard + force push，避免重写远端历史）
  - `js/scene-init.js` 恢复：太阳重新用官方默认 lensflare0.png（带黄圈+十字光芒），删除替换贴图 + scale [3,3,1] 的 27 行代码
  - 远端 `1ee7448..9f659f4`
